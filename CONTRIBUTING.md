# Contributing

## Commit and push policy

- Every commit must use the repository author name `creatawork`.
- Every commit must use `113406486+creatawork@users.noreply.github.com`.
- Do not add AI agent names, generated-by attribution, `Co-authored-by` trailers, or other AI-related metadata to commit messages.
- Review the staged file list before committing. Push only commits that follow these rules.

The repository-local Git configuration is the source of truth:

```text
user.name=creatawork
user.email=113406486+creatawork@users.noreply.github.com
```
