server {
  listen 80;
  server_name gallery.vie-vibe.cn;
  location /api/ { proxy_pass http://gallery-api:8080; proxy_set_header Host $host; proxy_set_header X-Forwarded-Proto $scheme; }
  location / { root /usr/share/nginx/html; try_files $uri $uri/ /index.html; }
}
